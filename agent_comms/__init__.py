"""Infrastructure for the agent communication network study.

Subpackages, one per infrastructure component described in PLAN.md:

- message_protocol: the MCP message protocol (component 1).
- parser: the session parser (component 2, not yet implemented).
- task_generator: the task generator (component 3, not yet implemented).
- runner: the experiment runner (component 4, not yet implemented).
"""
